# ESP provider: brevo | mailchimp | hubspot | sendgrid | convertkit | resend
ESP_PROVIDER=brevo

# Your ESP API key
ESP_API_KEY=xkeysib-YOUR_KEY_HERE

# Allowed origins (comma-separated). Requests from other origins are rejected.
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
